# Display the datasets of this scenario

from nested import product_db, dealer_db
import json

for p in product_db:
  print(json.dumps(p,sort_keys=True,indent=4))

print()

for d in dealer_db:
  print(d)
